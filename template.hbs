digraph {{title}} {
    rankdir = "LR";
    {{#each nodes}}
        {{name}}[shape=rectangle, style=filled, color="black", fillcolor="orange", {{#if caption}} label="{{caption}}"{{/if}}];
    {{/each}}
    {{#each edges}}
        {{from}} -> {{to}};
    {{/each}}
}